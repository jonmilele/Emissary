# Secrets
.env
html/secrets.inc.php

# Installer lock
html/.installed

# Runtime data
html/turntime.txt
html/turninterval.txt
html/config.inc.php

# Local overrides (not in repo)
docker-compose.override.yml

# Production forbidden words (real list managed on server)
# The repo ships data/forbidden_words.txt as an example template
!html/data/forbidden_words.txt

# MySQL data volume
mysql_data/

# macOS
.DS_Store
**/.DS_Store
